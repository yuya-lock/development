<% @page_title = "投稿詳細" %>
<h1><%= @page_title %></h1>

<% if current_user %>
  <div class="toolbar"><%= link_to "編集" %></div>
<% end %>

<table class="attr">
  <tr>
    <td><%= @post.university %></td>
  </tr>
    <td><%= @post.circle_name %></td>
  <tr>
    <td><%= simple_format(@post.body) %></td>
  </tr>
</table>